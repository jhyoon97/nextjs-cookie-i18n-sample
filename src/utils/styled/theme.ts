export type Palette = typeof light;

const light = {
  bg: "#fff",
  tabs: {
    bgActive: "#333",
    bgNormal: "#f9f9f9",
    textActive: "#fff",
    textNormal: "#000",
  },
};

// const dark: Palette = {}};

export default {
  light,
};
